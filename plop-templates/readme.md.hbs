# @myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }}

[![@myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }} on npm](https://img.shields.io/npm/v/@myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }}?style=for-the-badge)](https://npmjs.com/package/@myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }})

## Install

You can install `@myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }}` via your package manager of choice.

### npm

```bash
npm install --save-dev @myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }}
```

### yarn

```shell
yarn add --dev @myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }}
```

### pnpm

```shell
pnpm add --save-dev @myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }}
```

## Usage

Extend the config in your [ESLint configuration file].

Example: `.eslintrc`

```json
{
  "extends": "@myparcel-eslint/eslint-{{ type }}-{{ kebabCase name }}"
}
```

[ESLint configuration file]: https://eslint.org/docs/user-guide/configuring
